
# Compression and Codebooks

Semantic codebooks map canonical terms to compact stable identifiers. Unknown codes fail closed, duplicate mappings are invalid, and dictionary version skew is treated as an explicit error.
