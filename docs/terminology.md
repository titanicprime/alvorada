
# Terminology

Every canonical term has a stable identifier in `dictionary/canonical-dictionary.json`. Terms are append-oriented, never silently renamed, and changes are recorded in `dictionary/changelog.jsonl`.
